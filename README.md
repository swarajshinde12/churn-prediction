# churn-prediction
A desktop-based machine learning application that predicts customer churn from uploaded CSV data. It uses a pre-trained Random Forest model to automatically preprocess data and display accuracy, evaluation metrics, and visual insights like confusion matrix and feature importance in a single GUI.
